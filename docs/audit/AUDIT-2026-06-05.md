# AccessGate Org — Consolidated Audit (2026-06-05)

Scope: all AccessGate-lineage repositories, local working copies and remote/origin
(GitHub orgs `accessgate` + `policyfront`, user `ArmanAvanesyan`). Produced from a
multi-agent audit plus direct git/`gh` inspection. Secrets are redacted throughout.

## 1. Repository inventory

### Local (workspace `C:\users\arman\workspaces\accessgate\repos`)

| Repo | Git? | Branch | Dirty | Remote (origin) | Notes |
|------|------|--------|-------|-----------------|-------|
| `accessgate` | yes | `main` | 71 modified + 114 untracked | `github.com/accessgate/accessgate` | Canonical core. Healthy code, messy worktree. |
| `accessgate-clean` | worktree | detached `c03aab1` | 0 | (worktree of `accessgate`) | **Git worktree** of accessgate at `main` HEAD, clean. Not a separate repo. |
| `authsentinel` | yes | `main` | 171 deletions + 37 modified + 7 untracked | `github.com/ArmanAvanesyan/authsentinel` | Legacy ancestor. Large uncommitted removal of Go runtime + SDK packages. |
| `policyfront/*` (18 dirs) | mixed | — | — | mostly none / archived | Working-tree source for ecosystem split repos; remote copies are archived. |

### Remote

- **`gh` CLI** v2.83.2, authenticated as `ArmanAvanesyan`, scopes: `gist, project, read:org, repo, workflow`. Full read + push + project + workflow access.
- **org `accessgate`**: `accessgate` (public, Go, active), `authsentinel` (public, Go, active).
- **user `ArmanAvanesyan`**: `authsentinel` (public, active) — a **second, duplicate** authsentinel (identical `pushedAt` 2026-03-19). Local `repos/authsentinel` points here.
- **org `policyfront`**: 18 repos, **all archived (public)** — policyfront, policyfront-docker, policyfront-helm-chart, policy-bundles, playground, plugin-{caddy,traefik,krakend}, sdk-{dotnet,fastapi,flutter,go,nodejs,python,reactjs,typescript,web}, policyfront-observability.
- **`accessgate/accessgate`**: 0 open issues, 0 open PRs. Project **"AccessGate Consolidation" (#1, private, 27 items, 14 fields)**. 26 labels, 6 issue templates, `ci.yaml` (test + golangci-lint + buf + GoReleaser) all configured.

## 2. Reconciled lineage facts (corrections to docs)

1. **`accessgate-clean` is a git worktree**, not a stale fork — `repos/accessgate/.git/worktrees/accessgate-clean`, detached at `main` HEAD, clean. No unique data. Safe to `git worktree remove`.
2. **Duplicate `authsentinel`**: both `accessgate/authsentinel` and `ArmanAvanesyan/authsentinel` exist, identical and active. `docs/REPO-MAP.md` / `LINEAGE.md` cite `ArmanAvanesyan/authsentinel` (= local origin). The org copy is undocumented. **Decision needed** (Phase 3, confirmation-gated): pick one canonical legacy home, archive the other.
3. **`policyfront/*` are already archived remotely** — there is nothing to push or clean there; treat as frozen history. Local trees are read-only reference.

## 3. Core code quality (`accessgate`) — healthy

- **Naming drift**: effectively none in Go source. Only intentional back-compat: `AGENT_CONFIG` env fallback (`cmd/accessgate-auth/main.go:118`, `cmd/validateconfig/main.go`) with deprecation notes. No `policyfront_*` metrics, no `package policyfront`.
- **Secrets in source**: none. No committed `.env`, no hardcoded keys.
- **Tech debt**: 4 legitimate TODOs, all in the unfinished `internal/graphql` + `internal/grpc` adapter stubs.
- **Tests**: ~88% test-file ratio (84 test files / 95 source). Strong across `internal/*` and `pkg/*`. **Gap: `pkg/token` has no unit tests** — auth-critical (JWKS source, parser, validator, claims). [P0]
- **CI/release**: `ci.yaml` runs proto lint+breaking, codegen check, `go test -race -short` + coverage, golangci-lint, GoReleaser. **Missing**: vuln/SAST scanning (govulncheck/CodeQL), `dependabot.yml`, SBOM. [P1]

## 4. Working-tree state (the real mess)

### `accessgate`
- **71 modified tracked files** — coherent in-progress development across `internal/authz` (6), `docs` (5), `internal/proxy` (4), `internal/grpc` (3), `internal/auth/service` (3), tests, `internal/policy`, plugins, and `proto/` (both `.proto` sources and generated `gen/go` + `gen/ts`). → commit in logical slices (Phase 2).
- **114 untracked** — **mixed**, must be classified, NOT blanket-deleted:
  - **Real source to KEEP/commit**: `cmd/accessgate-proxy/bootstrap.go` (proxy composition root), `cmd/accessgate-proxy/policy_adapter.go`, `internal/proxy/httpserver/policy_adapter.go`, `agents/` (operating model), `AGENTS.md`, `.github/ISSUE_TEMPLATE/{docs-gap,legacy-migration}.yml`, `.github/labels.yml`, `.github/pull_request_template.md`.
  - **Scratch to DELETE**: `.tmp_*` (14), `issue*.txt`/`issue*.graphql`/`issue*.log` (~60), `*.b64` (4), `engine_contract_cut.patch`, `authsentinel-tree.json`, `items.json`, `project-items.json`, `naming-drift-scan.txt`, `j*.txt`, `oc_test2.txt`, `test3.txt"`, `tmp_write_test.txt`, `.github/scratch.yml`, `.github/test.txt`, and root marker files (`SDK`, `bootstrap`, `ecosystem`, `observability`, `packaging`, `plugin`, `policy-assets`, `primary-product-shell`, `unclear`).
  - **Tooling dirs to gitignore**: `.aok/`, `.codex/`, `.cursor/`, `.go-test-tmp/`.
- **Risk**: `bootstrap.go` is untracked yet is the proxy entrypoint — a blanket `git clean` would break the build. [P0 handling care]

### `authsentinel`
- **171 deletions + 37 modified** uncommitted — a coherent removal of the embedded Go runtime (`pkg/{sdk,proxy,policy,plugins,grpc,observability}`, `internal/*`, `configs/*`) and JS/Flutter SDK packages (`packages/sdk/js/*`, `packages/sdk/flutter/*`). Consistent with extracting the ecosystem out of the monorepo. → investigate intent, commit on a branch with a clear message; do not discard. [P1]

## 5. Ecosystem duplication

- `authsentinel` (monorepo) historically contained the SDKs/plugins now split into `policyfront/*` (archived) and partially re-present in `repos/policyfront/*` working trees. With policyfront archived and authsentinel mid-teardown, the SDK/plugin source has up to three locations. **Action**: declare `policyfront/*` the frozen historical home, finish removing the duplicated trees from `authsentinel`, and document the single source of truth.

## 6. Documentation gaps (`accessgate`)

Present & good: `docs/{LINEAGE,REPO-MAP,GITHUB-GOVERNANCE,ACCESSGATE-CONSOLIDATION,ISSUE-TAXONOMY}.md`, root `README.md`, `configs/`, `proto/` READMEs.
**Missing** (Phase 4): `CONTRIBUTING.md`, `SECURITY.md`, `docs/ARCHITECTURE.md`, `docs/MIGRATION.md` (AuthSentinel/PolicyFront → AccessGate), `docs/adr/` (no decision records), `docs/ROADMAP.md`.

## 7. Security & hygiene — action required

- **[P0 — flagged, user-owned remediation] Embedded GitHub PAT.** All active repos' `origin` URLs embed a token (`https://<REDACTED>@github.com/...`). The token was also exposed in this session's environment. Per the user's decision this audit only **flags** it; no remote rewrite/rotation performed here.
  - **Rotation checklist (do this independently):**
    1. GitHub → Settings → Developer settings → Personal access tokens → **revoke** the exposed token.
    2. Create a replacement (fine-grained, least-privilege) or switch to SSH / `gh auth login` + credential helper.
    3. Rewrite remotes to token-free URLs: `git remote set-url origin https://github.com/<org>/<repo>.git` (per repo), or SSH `git@github.com:<org>/<repo>.git`.
    4. Audit the token's recent use in GitHub's security log.
- No other secrets found in tracked source.

## 8. Prioritized findings

| # | Pri | Repo | Finding |
|---|-----|------|---------|
| 1 | P0 | all | Embedded PAT in remotes + session exposure → rotate (user-owned). |
| 2 | P0 | accessgate | Real source untracked (`bootstrap.go` etc.) — classify carefully before any clean. |
| 3 | P0 | accessgate | `pkg/token` has no unit tests (auth-critical). |
| 4 | P1 | accessgate | 71 modified files uncommitted — commit in reviewed slices. |
| 5 | P1 | authsentinel | 171-file teardown uncommitted — commit on branch, don't drop. |
| 6 | P1 | accessgate | CI lacks vuln/SAST scanning + dependabot. |
| 7 | P1 | accessgate | Missing CONTRIBUTING / SECURITY / ARCHITECTURE / MIGRATION / ADRs / ROADMAP. |
| 8 | P2 | org | Duplicate `authsentinel` repos (org vs user) — pick one, archive other. |
| 9 | P2 | accessgate | `accessgate-clean` leftover worktree — remove. |
| 10 | P2 | ecosystem | SDK/plugin source duplicated across authsentinel/policyfront — declare single source of truth. |

## 9. Backups taken (pre-cleanup, 2026-06-05 23:10 UTC)

Under workspace `backups/` (outside all repos): verified `git bundle --all` for `accessgate` + `authsentinel`; unstaged-diff patches; untracked-file tarballs + lists; `accessgate-clean` tar. All restore points confirmed (`git bundle verify` OK).
